# Hidden Preview Builder 简明使用说明

发布人：教主

版本：v0.1.0

## 使用前

下载名称带 `full-portable` 的 ZIP 并完整解压。发布包已附带
`ffmpeg.exe` 与 `ffprobe.exe`，无需安装 FFmpeg；请保持这两个文件与
`HiddenPreviewBuilder.exe` 在同一文件夹，不要只复制主程序。

## 三步生成

1. 选择 **Video A**：这是最终正常播放的内容，必须是恒定帧率且只有一个
   视频轨和一个音轨。
2. 选择 **预览图片 / Video B**，再明确选择补黑边、铺满裁切或直接拉伸。
3. 选择输出文件夹并点击 **生成 fin.mp4**。该文件夹不能已经存在
   `fin.mp4`。

界面显示“本地结构与解码校验 PASS”后即可使用输出。失败时不会保留本次生成
的不完整 MP4，诊断资料会留在错误信息给出的系统临时目录。

## 重要说明

- 正常播放器应只播放 Video A。
- 如果平台缩略图路径忽略 MP4 edit list，可能采样隐藏预览。
- 如果平台应用或展平 edit list，缩略图仍会采样 Video A。
- 本工具不保证任何平台一定采用隐藏预览。
- 随包 FFmpeg 单独采用 GPL v3；许可与源码说明位于
  `third-party\ffmpeg\`。
- 请只处理你有权使用的媒体，并遵守目标平台服务条款。
